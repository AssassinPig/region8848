<%= form_for @category, url: @category.new_record? ? admin_categories_path : admin_category_path(@category), html: {class: "form-horizontal"} do |f| %>
    <%= f.text_field :name, size:40 %>
    <%= f.submit "保存类别"%>
<% end %>
