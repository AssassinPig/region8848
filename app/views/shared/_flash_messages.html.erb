<% [:success, :info, :warning, :danger].each do |type| %>
    <% if flash[type] %>
        <div class="alert <% alert-#{type} %>"
            <a class="close" data-dismiss="alert"></a> | &times;
            <%= flash[type] %> 
        </div>
    <% end %>
<% end %>
